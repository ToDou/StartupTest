<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="@dimen/main_item_width"
    android:layout_height="@dimen/main_item_width">

    <com.test.tudou.startuptest.ui.view.shapeview.BubbleImageView
        android:id="@+id/image_list_item_main"
        android:layout_width="@dimen/main_item_width"
        android:layout_height="@dimen/main_item_height"
        app:siArrowPosition="right"
        app:siTriangleHeight="10dp"
        />

    <TextView
        android:id="@+id/text_list_item_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textColor="@android:color/white"
        android:textSize="@dimen/text_size_xmedium"/>

</FrameLayout>